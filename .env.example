# UniTime currently does not require runtime environment variables.
# Browser data is stored in localStorage.
#
# If you later add Supabase, Firebase, or another backend, document only
# public client-safe variables here. Never commit real secrets.
#
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
